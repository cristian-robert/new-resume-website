# Portfolio Website Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a bold, expressive single-page portfolio website for Cristian-Robert Iosef showcasing professional experience, skills, certifications, education, and public GitHub projects.

**Architecture:** Section-scroll single page with sticky nav, smooth-scroll anchors, and scroll-triggered animations. All CV data is static TypeScript. GitHub repos fetched via ISR (once/day). Contact form via Formspree. No heavy JS animation libraries — CSS transitions + Intersection Observer only.

**Tech Stack:** Next.js 16, React 19, TypeScript (strict), Tailwind CSS 4, shadcn/ui, Lucide icons, React Hook Form, Zod, Formspree.

**Design doc:** `docs/plans/2026-03-13-portfolio-website-design.md`

**GitHub username:** `cristian-robert`

---

## Chunk 1: Foundation

### Task 1: Install dependencies and initialize shadcn/ui

**Files:**
- Modify: `package.json`
- Create: `components.json` (generated by shadcn init)
- Modify: `app/globals.css` (shadcn adds CSS variables)
- Create: `lib/utils.ts`

> **Note:** Use `/shadcn-ui` skill for initialization.

- [ ] **Step 1: Initialize shadcn/ui**

Run: `npx shadcn@latest init`

Select options:
- Style: New York
- Base color: Zinc
- CSS variables: Yes

This creates `components.json`, updates `globals.css`, and creates `lib/utils.ts` with the `cn()` helper.

- [ ] **Step 2: Install shadcn/ui components**

```bash
npx shadcn@latest add button card badge sheet accordion input textarea label sonner separator
```

- [ ] **Step 3: Install form dependencies**

```bash
npm install react-hook-form zod @hookform/resolvers
```

- [ ] **Step 4: Verify build**

Run: `npm run build`
Expected: Build succeeds with no errors.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: init shadcn/ui and install dependencies"
```

---

### Task 2: Create TypeScript data types and typed data files

**Files:**
- Create: `lib/data/types.ts`
- Create: `lib/data/profile.ts`
- Create: `lib/data/experience.ts`
- Create: `lib/data/skills.ts`
- Create: `lib/data/certifications.ts`
- Create: `lib/data/education.ts`
- Create: `lib/data/index.ts`

Source data: existing `.md` files in `lib/data/`. Convert to typed TypeScript exports. Keep `.md` files as reference for the cv-parser skill.

- [ ] **Step 1: Create types.ts**

```typescript
// lib/data/types.ts

export interface Profile {
  name: string;
  title: string;
  location: string;
  email: string;
  phone: string;
  linkedin: string;
  website: string;
  summary: string[];
}

export interface ExperienceEntry {
  company: string;
  role: string;
  location: string;
  period: string;
  duration: string;
  current: boolean;
  description: string;
  achievements: string[];
  technologies: string[];
}

export interface SkillCategory {
  name: string;
  skills: string[];
  emphasis: boolean;
}

export interface Certification {
  name: string;
}

export interface EducationEntry {
  university: string;
  degree: string;
  field: string;
  period: string;
}

export interface GitHubRepo {
  name: string;
  description: string | null;
  url: string;
  language: string | null;
  stars: number;
  forks: number;
  updatedAt: string;
}
```

- [ ] **Step 2: Create profile.ts**

```typescript
// lib/data/profile.ts
import type { Profile } from "./types";

export const profile: Profile = {
  name: "Cristian-Robert Iosef",
  title: "Senior QA Automation Engineer | Java | JavaScript/Typescript | Playwright | Selenium/Cucumber/RestAssured | CI/CD | SpringBoot",
  location: "Bucharest, Romania",
  email: "roby248@live.com",
  phone: "+40762167342",
  linkedin: "https://www.linkedin.com/in/cristianrobert-iosef",
  website: "https://www.criosef-resume.info/",
  summary: [
    "Test Automation Architect specializing in enterprise-grade frameworks across web, API, and performance domains. Expert in crafting multi-stack automation strategies using Java (Selenium, RestAssured) and JavaScript (Playwright, Cypress) technologies that seamlessly integrate with modern CI/CD pipelines.",
    "Proven success reducing testing cycles and improving quality metrics through intelligent automation solutions. Currently developing innovative approaches combining BDD practices with cloud-native testing and AI capabilities.",
    "AI enthusiast leveraging LLMs and AI tools to enhance testing efficiency, automate repetitive tasks, and create next-generation QA solutions that bridge traditional testing with cutting-edge AI technologies."
  ],
};
```

- [ ] **Step 3: Create experience.ts**

Extract all 5 roles from `lib/data/experience.md` into typed array. Each entry has company, role, location, period, duration, current flag, description, achievements array, and technologies array.

```typescript
// lib/data/experience.ts
import type { ExperienceEntry } from "./types";

export const experience: ExperienceEntry[] = [
  {
    company: "Deutsche Bank",
    role: "Senior Quality Assurance Automation Engineer",
    location: "Bucharest, Romania",
    period: "October 2019 - Present",
    duration: "6 years 6 months",
    current: true,
    description: "Developed enterprise automation frameworks from scratch using Java/Spring Boot and TypeScript/Playwright with CI/CD integrations.",
    achievements: [
      "Developed automation framework from scratch using Java/Spring Boot/Selenium/Cucumber/RestAssured for both front-end and backend with OracleSQL database integration with CI/CD integrations in TeamCity",
      "Developed automation framework from scratch using TypeScript/Playwright for both front-end and backend with OracleSQL database integration and GraphQL APIs with CI/CD in GitHub Actions and Google Cloud",
      "Developed performance tests using JMeter and K6",
      "Responsible for defining the automation framework and automating test cases",
      "Active participation in continuous improvement of automation strategy and framework",
      "Performing code reviews on automated tests",
      "Conducting performance testing using JMeter",
    ],
    technologies: [
      "Java", "Spring Boot", "SpringData JPA", "Selenium", "Cucumber", "RestAssured", "TestNG", "JUnit",
      "TypeScript", "Playwright", "JMeter", "K6",
      "OracleSQL", "PostgreSQL",
      "TeamCity", "GitLab CI", "GitHub Actions", "Google Cloud",
      "Maven", "Jira", "Bitbucket", "Confluence", "OpenShift",
    ],
  },
  {
    company: "Twispay",
    role: "QA Automation Engineer",
    location: "Bucharest, Romania",
    period: "January 2019 - October 2019",
    duration: "10 months",
    current: false,
    description: "Created test plans and automated functional suites for a payment platform covering Back Office, Payment, and API areas.",
    achievements: [
      "Created test plans and test cases on all areas of the platform (Back Office, Payment, API)",
      "Designed and implemented automated functional suites in Yii-Codeception",
      "Performed tests against builds and test procedures",
      "Debugging and identifying code errors by checking server error logs",
    ],
    technologies: [
      "PHP", "Yii-Codeception", "GitLab", "Git", "MySQL",
      "Postman (REST)", "Youtrack",
      "Security vulnerabilities scanning tools (OWASP top 10)",
    ],
  },
  {
    company: "2Checkout",
    role: "Quality Assurance Automation Engineer",
    location: "Bucharest, Romania",
    period: "June 2018 - December 2018",
    duration: "7 months",
    current: false,
    description: "Handled manual and automated testing at all levels with focus on security and compliance testing at Avangate/2Checkout.",
    achievements: [
      "Handled manual and automated testing at all levels",
      "Focus on security and compliance testing",
      "Developed automated testing in PHPUnit and Behat covering the entire application",
      "Covered diverse use cases including negative testing, business rules scenarios and performance",
      "Followed ISTQB best practices and techniques",
    ],
    technologies: [
      "PHP", "Behat", "Gherkin", "PHPUnit",
      "Jenkins", "Git", "MySQL", "Kibana",
      "REST", "SOAP", "JSON APIs",
      "JIRA", "Confluence", "Redmine",
      "Security vulnerabilities scanning (OWASP top 10)",
    ],
  },
  {
    company: "eMAG",
    role: "QA Engineer",
    location: "Bucharest, Romania",
    period: "August 2017 - June 2018",
    duration: "11 months",
    current: false,
    description: "Designed and implemented automated test suites in Codeception using Selenium WebDriver for Romania's largest e-commerce platform.",
    achievements: [
      "Creating test plans and test cases",
      "Designing and implementing automated test suites in Codeception using Selenium WebDriver",
      "Identify, analyze, and document issues from running automated test scripts",
      "Maintain automation tests",
      "Participate in daily meetings and estimate effort for testing future tasks",
    ],
    technologies: [
      "Codeception", "Selenium WebDriver", "PHP",
      "Jira", "Bitbucket", "Git",
      "SQL", "HeidiSQL", "Linux",
    ],
  },
  {
    company: "Ubisoft",
    role: "Game Tester",
    location: "Bucharest, Romania",
    period: "September 2015 - August 2017",
    duration: "2 years",
    current: false,
    description: "QC testing on multiple platforms. Task Leader for a mini-team of 8 people.",
    achievements: [
      "QC testing on XBOX One, PS4 and PC",
      "Building test cases on Excel and TestRail",
      "JIRA Advanced Reports and Bug Base Management",
      "Task Leader for a mini-team of 8 people",
      "Weekly Feature reports",
      "Follow-up of versions from scratch to end of project",
    ],
    technologies: [
      "Excel", "TestRail", "JIRA",
      "XBOX One", "PS4", "PC",
    ],
  },
];
```

- [ ] **Step 4: Create skills.ts**

```typescript
// lib/data/skills.ts
import type { SkillCategory } from "./types";

export const skills: SkillCategory[] = [
  {
    name: "Programming Languages",
    skills: ["Java", "JavaScript", "TypeScript", "PHP"],
    emphasis: false,
  },
  {
    name: "Frontend",
    skills: ["React", "Next.js"],
    emphasis: false,
  },
  {
    name: "Testing & Automation",
    skills: [
      "Selenium", "Playwright", "Cypress", "WebdriverIO",
      "Cucumber", "RestAssured", "TestNG", "JUnit",
      "Codeception", "Behat", "JMeter", "K6",
    ],
    emphasis: true,
  },
  {
    name: "Backend & Frameworks",
    skills: ["Spring Boot", "SpringData JPA"],
    emphasis: false,
  },
  {
    name: "CI/CD & DevOps",
    skills: [
      "Jenkins", "TeamCity", "GitLab CI", "GitHub Actions",
      "Google Cloud", "Docker", "OpenShift",
    ],
    emphasis: false,
  },
  {
    name: "Databases",
    skills: ["PostgreSQL", "MySQL", "OracleSQL"],
    emphasis: false,
  },
  {
    name: "Tools & Platforms",
    skills: [
      "Git", "BitBucket", "Jira", "Confluence",
      "Azure DevOps", "HP ALM", "TestRail",
      "Maven", "Postman", "Kibana",
    ],
    emphasis: false,
  },
  {
    name: "AI & LLMs",
    skills: [
      "Claude", "OpenAI (ChatGPT/GPT-4)", "Gemini", "Deepseek",
      "GitHub Copilot", "Cursor", "Augment Code",
      "Prompt Engineering", "MCP Integration", "AI Agent Workflows",
    ],
    emphasis: true,
  },
  {
    name: "Spoken Languages",
    skills: ["Romanian (Native)", "English (Professional Working)"],
    emphasis: false,
  },
];
```

- [ ] **Step 5: Create certifications.ts**

```typescript
// lib/data/certifications.ts
import type { Certification } from "./types";

export const certifications: Certification[] = [
  { name: "Certificat de competente profesionale nivel 3 - Tehnician in activitati economice" },
  { name: "Java: Automated API Testing with REST Assured" },
  { name: "Java Object-Oriented Programming" },
  { name: "Advanced Selenium: Support Classes" },
  { name: "Learning Spring with Spring Boot" },
];
```

- [ ] **Step 6: Create education.ts**

```typescript
// lib/data/education.ts
import type { EducationEntry } from "./types";

export const education: EducationEntry[] = [
  {
    university: 'Universitatea "Hyperion" din Bucharest',
    degree: "Master's degree",
    field: "Leadership and Human Resources Management",
    period: "2017 - 2019",
  },
  {
    university: "Universitatea Hyperion",
    degree: "Bachelor's degree",
    field: "Management",
    period: "2014 - 2017",
  },
];
```

- [ ] **Step 7: Create index.ts barrel export**

```typescript
// lib/data/index.ts
export { profile } from "./profile";
export { experience } from "./experience";
export { skills } from "./skills";
export { certifications } from "./certifications";
export { education } from "./education";
export type {
  Profile,
  ExperienceEntry,
  SkillCategory,
  Certification,
  EducationEntry,
  GitHubRepo,
} from "./types";
```

- [ ] **Step 8: Verify build**

Run: `npm run build`
Expected: Build succeeds with no type errors.

- [ ] **Step 9: Commit**

```bash
git add lib/data/types.ts lib/data/profile.ts lib/data/experience.ts lib/data/skills.ts lib/data/certifications.ts lib/data/education.ts lib/data/index.ts
git commit -m "feat: add typed data layer for CV content"
```

---

### Task 3: Create GitHub fetcher and copy CV to public

**Files:**
- Create: `lib/github.ts`
- Copy: `docs/Profile.pdf` → `public/Cristian-Robert-Iosef-CV.pdf`

- [ ] **Step 1: Create lib/github.ts**

```typescript
// lib/github.ts
import type { GitHubRepo } from "@/lib/data/types";

const GITHUB_USERNAME = "cristian-robert";
const GITHUB_API = "https://api.github.com";

export async function fetchGitHubRepos(): Promise<GitHubRepo[]> {
  const res = await fetch(
    `${GITHUB_API}/users/${GITHUB_USERNAME}/repos?sort=updated&per_page=30&type=owner`,
    {
      headers: {
        Accept: "application/vnd.github.v3+json",
      },
      next: { revalidate: 86400 },
    }
  );

  if (!res.ok) {
    console.error(`GitHub API error: ${res.status}`);
    return [];
  }

  const data = await res.json();

  return data
    .filter((repo: Record<string, unknown>) => !repo.fork)
    .map((repo: Record<string, unknown>) => ({
      name: repo.name as string,
      description: (repo.description as string) ?? null,
      url: repo.html_url as string,
      language: (repo.language as string) ?? null,
      stars: repo.stargazers_count as number,
      forks: repo.forks_count as number,
      updatedAt: repo.updated_at as string,
    }))
    .sort((a: GitHubRepo, b: GitHubRepo) => b.stars - a.stars);
}

export { GITHUB_USERNAME };
```

- [ ] **Step 2: Copy CV PDF to public directory**

```bash
cp docs/Profile.pdf public/Cristian-Robert-Iosef-CV.pdf
```

- [ ] **Step 3: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 4: Commit**

```bash
git add lib/github.ts public/Cristian-Robert-Iosef-CV.pdf
git commit -m "feat: add GitHub repo fetcher and downloadable CV"
```

---

### Task 4: Set up theme, globals.css, and layout.tsx

**Files:**
- Modify: `app/globals.css`
- Modify: `app/layout.tsx`

> **Note:** The executing agent MUST ask the user which design skill to use (`/frontend-design`, `/frontend-aesthetics`, or `/ui-ux-pro-max`) before implementing this task. The design skill will guide color palette, typography, and animation decisions. The below is a structural starting point — the design skill overrides visual choices.

- [ ] **Step 1: Update globals.css with animation keyframes and theme variables**

Add to `globals.css` (after shadcn/ui variables):
- CSS `@keyframes` for: `fadeInUp`, `fadeInLeft`, `fadeInRight`, `scaleIn`, `float` (for background orbs)
- `prefers-reduced-motion` media query that disables animations
- Smooth scroll on `html`
- Custom properties for accent colors (to be finalized by design skill)

Key animations to define (both `@keyframes` AND explicit utility classes, since Tailwind v4 cannot generate dynamic class names like `animate-fadeInUp` from string interpolation):

```css
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(24px); }
  to { opacity: 1; transform: translateY(0); }
}
@keyframes fadeInLeft {
  from { opacity: 0; transform: translateX(-24px); }
  to { opacity: 1; transform: translateX(0); }
}
@keyframes fadeInRight {
  from { opacity: 0; transform: translateX(24px); }
  to { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(0.9); }
  to { opacity: 1; transform: scale(1); }
}
@keyframes float {
  0%, 100% { transform: translate(0, 0); }
  50% { transform: translate(20px, -20px); }
}

/* Explicit animation utility classes — required because Tailwind v4
   cannot discover dynamic class names from string interpolation. */
.animate-fadeInUp { animation: fadeInUp 0.6s ease-out; }
.animate-fadeInLeft { animation: fadeInLeft 0.6s ease-out; }
.animate-fadeInRight { animation: fadeInRight 0.6s ease-out; }
.animate-scaleIn { animation: scaleIn 0.5s ease-out; }
.animate-float { animation: float 6s ease-in-out infinite; }

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}

html { scroll-behavior: smooth; }
```

- [ ] **Step 2: Update layout.tsx**

Update metadata, fonts, add Sonner toast provider:

```typescript
// app/layout.tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Cristian-Robert Iosef | Senior QA Automation Engineer",
  description:
    "Test Automation Architect specializing in enterprise-grade frameworks across web, API, and performance domains.",
  keywords: [
    "QA Automation", "Test Automation", "Playwright", "Selenium",
    "Java", "TypeScript", "CI/CD", "Senior Engineer",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
```

- [ ] **Step 3: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 4: Commit**

```bash
git add app/globals.css app/layout.tsx
git commit -m "feat: set up theme, animations, and layout metadata"
```

---

## Chunk 2: Shared Components + Hero

### Task 5: Create ScrollAnimator component

**Files:**
- Create: `components/ScrollAnimator.tsx`

A reusable client component wrapping Intersection Observer. Adds a CSS class when the element enters the viewport to trigger animations.

- [ ] **Step 1: Create ScrollAnimator.tsx**

```typescript
// components/ScrollAnimator.tsx
"use client";

import { useEffect, useRef, useState } from "react";
import { cn } from "@/lib/utils";

// Explicit class mapping — Tailwind v4 cannot discover dynamic class names
// from string interpolation like `animate-${animation}`. These map to the
// utility classes defined in globals.css.
const ANIMATION_CLASSES = {
  fadeInUp: "animate-fadeInUp",
  fadeInLeft: "animate-fadeInLeft",
  fadeInRight: "animate-fadeInRight",
  scaleIn: "animate-scaleIn",
} as const;

type AnimationName = keyof typeof ANIMATION_CLASSES;

interface ScrollAnimatorProps {
  children: React.ReactNode;
  className?: string;
  animation?: AnimationName;
  delay?: number; // ms
  threshold?: number;
  once?: boolean;
}

export function ScrollAnimator({
  children,
  className,
  animation = "fadeInUp",
  delay = 0,
  threshold = 0.1,
  once = true,
}: ScrollAnimatorProps) {
  const ref = useRef<HTMLDivElement>(null);
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    const el = ref.current;
    if (!el) return;

    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setIsVisible(true);
          if (once) observer.unobserve(el);
        } else if (!once) {
          setIsVisible(false);
        }
      },
      { threshold }
    );

    observer.observe(el);
    return () => observer.disconnect();
  }, [threshold, once]);

  return (
    <div
      ref={ref}
      className={cn(
        "opacity-0",
        isVisible && ANIMATION_CLASSES[animation],
        isVisible && "opacity-100",
        className
      )}
      style={{
        animationDelay: isVisible ? `${delay}ms` : undefined,
        animationFillMode: "both",
      }}
    >
      {children}
    </div>
  );
}
```

> **Note:** The `ANIMATION_CLASSES` map uses static strings that match the utility classes defined in `globals.css` (Task 4). This avoids Tailwind v4's inability to discover dynamically constructed class names.

- [ ] **Step 2: Verify build**

Run: `npm run build`
Expected: Build succeeds (component not yet used, just compiled).

- [ ] **Step 3: Commit**

```bash
git add components/ScrollAnimator.tsx
git commit -m "feat: add ScrollAnimator intersection observer component"
```

---

### Task 6: Create Navigation component

**Files:**
- Create: `components/Navigation.tsx`

Sticky header with scroll-spy, mobile hamburger (shadcn Sheet), backdrop-blur on scroll, and "Download CV" button. Client component (needs scroll event + state).

- [ ] **Step 1: Create Navigation.tsx**

```typescript
// components/Navigation.tsx
"use client";
```

Sections for the nav links:
```typescript
const NAV_LINKS = [
  { label: "About", href: "#about" },
  { label: "Skills", href: "#skills" },
  { label: "Experience", href: "#experience" },
  { label: "Projects", href: "#projects" },
  { label: "Education", href: "#education" },
  { label: "Contact", href: "#contact" },
] as const;
```

Component requirements:
- Uses `useState` for `scrolled` (boolean, true when `scrollY > 50`) and `activeSection` (string)
- Uses `useEffect` with scroll event listener (passive) for scroll detection
- Uses `IntersectionObserver` to track which section is in view for scroll-spy
- Desktop: horizontal link list + "Download CV" Button
- Mobile: Sheet with hamburger trigger (Menu icon from lucide-react), vertical link list inside
- Clicking a nav link on mobile closes the Sheet
- "Download CV" links to `/Cristian-Robert-Iosef-CV.pdf` with `download` attribute

Styling:
- `fixed top-0 w-full z-50`
- When not scrolled: `bg-transparent`
- When scrolled: `bg-background/80 backdrop-blur-md border-b`
- Active nav link gets accent color

- [ ] **Step 2: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
git add components/Navigation.tsx
git commit -m "feat: add sticky navigation with scroll-spy and mobile menu"
```

---

### Task 7: Create Hero section with typed text hook

**Files:**
- Create: `hooks/useTypedText.ts`
- Create: `components/sections/Hero.tsx`

- [ ] **Step 1: Create useTypedText hook**

```typescript
// hooks/useTypedText.ts
"use client";

import { useState, useEffect, useCallback } from "react";

interface UseTypedTextOptions {
  strings: string[];
  typingSpeed?: number;   // ms per character
  deletingSpeed?: number; // ms per character
  pauseDuration?: number; // ms pause after typing complete
}

export function useTypedText({
  strings,
  typingSpeed = 80,
  deletingSpeed = 40,
  pauseDuration = 2000,
}: UseTypedTextOptions): string {
  const [text, setText] = useState("");
  const [stringIndex, setStringIndex] = useState(0);
  const [isDeleting, setIsDeleting] = useState(false);

  const tick = useCallback(() => {
    const currentString = strings[stringIndex];

    if (!isDeleting) {
      setText(currentString.slice(0, text.length + 1));
      if (text.length + 1 === currentString.length) {
        setTimeout(() => setIsDeleting(true), pauseDuration);
        return;
      }
    } else {
      setText(currentString.slice(0, text.length - 1));
      if (text.length - 1 === 0) {
        setIsDeleting(false);
        setStringIndex((prev) => (prev + 1) % strings.length);
        return;
      }
    }
  }, [text, stringIndex, isDeleting, strings, pauseDuration]);

  useEffect(() => {
    const speed = isDeleting ? deletingSpeed : typingSpeed;
    const timer = setTimeout(tick, speed);
    return () => clearTimeout(timer);
  }, [tick, isDeleting, typingSpeed, deletingSpeed]);

  return text;
}
```

- [ ] **Step 2: Create Hero.tsx**

```typescript
// components/sections/Hero.tsx
"use client";
```

Component requirements:
- Full viewport height section (`min-h-screen`)
- `id="hero"` for scroll anchor
- Imports `profile` from `@/lib/data`
- Uses `useTypedText` with strings: `["Test Automation Architect", "AI Enthusiast", "QA Engineer", "Framework Builder"]`
- Large bold heading with `profile.name`
- Typed text line with blinking cursor (CSS `border-right` animation)
- 2-3 line summary from `profile.summary[0]` (truncated/shortened)
- Two CTA buttons: "View My Work" → `#experience`, "Get in Touch" → `#contact`
- Background: animated gradient orbs using CSS `@keyframes float` (2-3 absolutely positioned divs with large border-radius, gradient fills, blur filter, low opacity)
- Responsive: stacks vertically on mobile, orbs simplified

- [ ] **Step 3: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 4: Commit**

```bash
git add hooks/useTypedText.ts components/sections/Hero.tsx
git commit -m "feat: add Hero section with typed text effect"
```

---

## Chunk 3: Content Sections

### Task 8: Create About section

**Files:**
- Create: `components/sections/About.tsx`

- [ ] **Step 1: Create About.tsx**

```typescript
// components/sections/About.tsx
```

Component requirements:
- `id="about"` for scroll anchor
- Imports `profile` and `skills` from `@/lib/data`
- Section heading: "About Me"
- Displays all `profile.summary` paragraphs (the full 3-paragraph bio)
- Below the summary, a compact "Tech Stack Overview" showing the top skill categories as a visual highlight (e.g., key categories like Testing & Automation, CI/CD, AI & LLMs as icon+label items or a horizontal badge strip)
- Wrapped in `ScrollAnimator` with `fadeInUp`
- Responsive: single column, comfortable reading width (`max-w-3xl` centered or similar)

- [ ] **Step 2: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
git add components/sections/About.tsx
git commit -m "feat: add About section with summary and tech stack overview"
```

---

### Task 9: Create Skills section (was Task 8)

**Files:**
- Create: `components/sections/Skills.tsx`

- [ ] **Step 1: Create Skills.tsx**

```typescript
// components/sections/Skills.tsx
```

Component requirements:
- `id="skills"` for scroll anchor
- Imports `skills` from `@/lib/data`
- Section heading: "Skills & Expertise"
- Maps over `skills` array, renders a Card for each category
- Responsive grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
- Each Card:
  - Category name as CardHeader
  - Colored left border (use a consistent accent per category or alternating)
  - Skills as Badge components inside CardContent
  - Categories with `emphasis: true` get a subtle accent background or larger size
- Each Card wrapped in `ScrollAnimator` with staggered delay: `index * 100`ms
- Badges use `variant="secondary"` or custom styling for the bold/expressive look

- [ ] **Step 2: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
git add components/sections/Skills.tsx
git commit -m "feat: add Skills section with categorized animated cards"
```

---

### Task 10: Create Experience section

**Files:**
- Create: `components/sections/Experience.tsx`

- [ ] **Step 1: Create Experience.tsx**

```typescript
// components/sections/Experience.tsx
```

Component requirements:
- `id="experience"` for scroll anchor
- Imports `experience` from `@/lib/data`
- Section heading: "Experience"
- Vertical timeline layout:
  - A vertical line (`border-left` or pseudo-element) running along the left side
  - Each entry is positioned relative to the line with a dot/node on the line
  - On desktop (lg+): entries alternate left/right of center line
  - On mobile: all entries on the right of a left-edge line
- Each entry shows:
  - Company name (bold, accent color)
  - Role title
  - Period + duration as a Badge
  - Location
  - Current role gets a "Current" Badge with distinct styling
  - Brief `description` text visible by default
- Expandable section using shadcn Accordion:
  - "View Details" trigger
  - Expands to show full `achievements` list and `technologies` as Badges
- Each entry wrapped in `ScrollAnimator`:
  - Desktop: alternating `fadeInLeft` / `fadeInRight`
  - Mobile: all `fadeInLeft`
  - Staggered delay: `index * 150`ms

- [ ] **Step 2: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
git add components/sections/Experience.tsx
git commit -m "feat: add Experience section with timeline and expandable details"
```

---

### Task 11: Create Projects section

**Files:**
- Create: `components/sections/Projects.tsx`

- [ ] **Step 1: Create Projects.tsx**

```typescript
// components/sections/Projects.tsx
```

Component requirements:
- `id="projects"` for scroll anchor
- Receives `repos: GitHubRepo[]` as props (passed from page.tsx server component)
- Section heading: "Open Source & Projects" with a link to `https://github.com/cristian-robert`
- Responsive card grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
- Each Card:
  - Repo name as heading with external link icon
  - Description text (or "No description" fallback)
  - Language with colored dot (use a lookup map for common GitHub language colors: TypeScript → blue, Java → orange, JavaScript → yellow, Python → green, etc.)
  - Star count with Star icon, fork count with GitFork icon (from lucide-react)
  - "Last updated" relative date
  - Hover effect: slight translateY(-2px) + shadow increase + accent border glow
  - Entire card is a link to the repo URL, `target="_blank" rel="noopener noreferrer"`
- Each card wrapped in `ScrollAnimator` with stagger delay
- If `repos` is empty, show a subtle "Loading projects..." or "Check out my GitHub" fallback

- [ ] **Step 2: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
git add components/sections/Projects.tsx
git commit -m "feat: add Projects section with GitHub repo cards"
```

---

### Task 12: Create Certifications and Education sections

**Files:**
- Create: `components/sections/Certifications.tsx`
- Create: `components/sections/Education.tsx`

- [ ] **Step 1: Create Certifications.tsx**

```typescript
// components/sections/Certifications.tsx
```

Component requirements:
- `id="certifications"` for scroll anchor
- Imports `certifications` from `@/lib/data`
- Section heading: "Certifications"
- Responsive grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
- Each cert as a compact Card with:
  - Award icon (from lucide-react)
  - Certification name
- Each wrapped in `ScrollAnimator` with stagger

- [ ] **Step 2: Create Education.tsx**

```typescript
// components/sections/Education.tsx
```

Component requirements:
- `id="education"` for scroll anchor
- Imports `education` from `@/lib/data`
- Section heading: "Education"
- Two cards: `grid-cols-1 md:grid-cols-2`
- Each Card:
  - GraduationCap icon (lucide-react)
  - University name
  - Degree + field
  - Period as Badge
- Each wrapped in `ScrollAnimator` with stagger

- [ ] **Step 3: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 4: Commit**

```bash
git add components/sections/Certifications.tsx components/sections/Education.tsx
git commit -m "feat: add Certifications and Education sections"
```

---

## Chunk 4: Contact, Footer, Assembly

### Task 13: Create Contact section

**Files:**
- Create: `components/sections/Contact.tsx`

> **Note:** The user needs to create a Formspree account at https://formspree.io and get a form endpoint URL. Use a placeholder `FORMSPREE_ENDPOINT` that must be replaced. Alternatively, use an environment variable `NEXT_PUBLIC_FORMSPREE_ID`.

- [ ] **Step 0: Create .env.example and .env.local**

Create `.env.example` (committed to git):
```
NEXT_PUBLIC_FORMSPREE_ID=your_formspree_form_id
```

Create `.env.local` (gitignored, local only):
```
NEXT_PUBLIC_FORMSPREE_ID=<actual-id-from-formspree>
```

If the user hasn't set up Formspree yet, use a placeholder. The form will show an error on submit but won't break the build.

- [ ] **Step 1: Create Contact.tsx**

```typescript
// components/sections/Contact.tsx
"use client";
```

Component requirements:
- `id="contact"` for scroll anchor
- Imports `profile` from `@/lib/data`
- Split layout: `grid-cols-1 lg:grid-cols-2`
- **Left side — Contact Info:**
  - Email (Mail icon) — `mailto:` link
  - Phone (Phone icon) — `tel:` link
  - LinkedIn (Linkedin icon) — external link
  - Website (Globe icon) — external link
  - Location (MapPin icon) — text only
  - Each item wrapped in `ScrollAnimator` with `fadeInLeft` and stagger
- **Right side — Contact Form:**
  - Uses React Hook Form with Zod schema:
    ```typescript
    const contactSchema = z.object({
      name: z.string().min(2, "Name is required"),
      email: z.string().email("Invalid email"),
      subject: z.string().min(2, "Subject is required"),
      message: z.string().min(10, "Message must be at least 10 characters"),
    });
    ```
  - Fields: name (Input), email (Input), subject (Input), message (Textarea)
  - Submit Button with loading state (disabled + spinner while submitting)
  - **Form submission handler:**
    ```typescript
    import { toast } from "sonner";

    const onSubmit = async (data: ContactFormData) => {
      try {
        const res = await fetch(
          `https://formspree.io/f/${process.env.NEXT_PUBLIC_FORMSPREE_ID}`,
          {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify(data),
          }
        );
        if (!res.ok) throw new Error("Failed to send");
        toast.success("Message sent! I'll get back to you soon.");
        form.reset();
      } catch {
        toast.error("Something went wrong. Please try again or email me directly.");
      }
    };
    ```
  - On success: show success toast via Sonner, reset form
  - On error: show error toast
  - Wrapped in `ScrollAnimator` with `fadeInRight`

- [ ] **Step 2: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
git add components/sections/Contact.tsx .env.example
git commit -m "feat: add Contact section with form and info links"
```

---

### Task 14: Create Footer component

**Files:**
- Create: `components/Footer.tsx`

- [ ] **Step 1: Create Footer.tsx**

```typescript
// components/Footer.tsx
```

Component requirements:
- Imports `profile` from `@/lib/data`
- Imports `GITHUB_USERNAME` from `@/lib/github`
- Dark background, contrasting text
- Three elements in a row (stacked on mobile):
  - Name on the left
  - Copyright in the center: `© {new Date().getFullYear()} Cristian-Robert Iosef`
  - Social links on the right: GitHub, LinkedIn, Email — icon buttons linking to respective URLs
- Separator above the footer content

- [ ] **Step 2: Verify build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
git add components/Footer.tsx
git commit -m "feat: add Footer component"
```

---

### Task 15: Assemble page.tsx and verify full build

**Files:**
- Modify: `app/page.tsx`

- [ ] **Step 1: Rewrite page.tsx**

```typescript
// app/page.tsx
import { fetchGitHubRepos } from "@/lib/github";
import { Navigation } from "@/components/Navigation";
import { Hero } from "@/components/sections/Hero";
import { About } from "@/components/sections/About";
import { Skills } from "@/components/sections/Skills";
import { Experience } from "@/components/sections/Experience";
import { Projects } from "@/components/sections/Projects";
import { Certifications } from "@/components/sections/Certifications";
import { Education } from "@/components/sections/Education";
import { Contact } from "@/components/sections/Contact";
import { Footer } from "@/components/Footer";

export default async function Home() {
  const repos = await fetchGitHubRepos();

  return (
    <>
      <Navigation />
      <main>
        <Hero />
        <About />
        <Skills />
        <Experience />
        <Projects repos={repos} />
        <Certifications />
        <Education />
        <Contact />
      </main>
      <Footer />
    </>
  );
}
```

- [ ] **Step 2: Run build**

Run: `npm run build`
Expected: Build succeeds with no errors.

- [ ] **Step 3: Run lint**

Run: `npm run lint`
Expected: No lint errors.

- [ ] **Step 4: Run dev server and visually verify**

Run: `npm run dev`
Open: `http://localhost:3000`

Verify:
- All sections render
- Navigation scroll-spy works
- Mobile menu opens/closes
- Typed text animates in hero
- Scroll animations trigger on each section
- GitHub repos load
- Contact form validates
- Download CV button works
- No console errors

- [ ] **Step 5: Commit**

```bash
git add app/page.tsx
git commit -m "feat: assemble all sections into single-page layout"
```

---

## Design Skill Reminder

**IMPORTANT:** Before implementing Tasks 4, 7, 8, 9, 10, 11, 12, 13, and 14 (any task involving visual UI), the executing agent MUST ask the user which design skill to load:

1. `/frontend-design` — Full page/component creation with bold aesthetics
2. `/frontend-aesthetics` — Lightweight design guardrails
3. `/ui-ux-pro-max` — Design planning and exploration

The design skill guides color palette, typography, spacing, and visual treatment decisions. The code in this plan is structural — the design skill overrides aesthetic choices.

## Formspree Setup Reminder

Before the Contact section can fully work, the user must:
1. Create a free account at https://formspree.io
2. Create a new form
3. Get the form endpoint ID
4. Set it as `NEXT_PUBLIC_FORMSPREE_ID` in `.env.local`
