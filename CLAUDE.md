# Resume Portfolio Website

## Project Overview

Single-page interactive portfolio/resume website for Cristian-Robert Iosef. Showcases professional experience, skills, certifications, education, and public GitHub projects. Data is statically typed in TypeScript files, generated from `docs/Profile.pdf` via the `cv-parser` skill.

## Tech Stack

- **Framework:** Next.js 16, React 19, TypeScript (strict)
- **Styling:** Tailwind CSS 4, shadcn/ui
- **Data:** Static TypeScript files in `lib/data/`, GitHub API (public repos at build time)
- **Deployment:** Vercel
- **Package Manager:** npm

## Directory Structure

```
resume-website/
├── app/                # Next.js App Router (single page + layout)
│   ├── layout.tsx      # Root layout
│   ├── page.tsx        # Main single-page view
│   ├── globals.css     # Global styles + Tailwind
│   └── favicon.ico
├── components/         # React components (sections, UI)
│   ├── sections/       # Page sections (hero, skills, experience, etc.)
│   └── ui/             # shadcn/ui components
├── lib/                # Utilities and data
│   ├── data/           # Static typed CV data (generated by cv-parser skill)
│   └── github.ts       # GitHub API fetcher (build-time)
├── public/             # Static assets (images, resume PDF download)
├── docs/               # Source CV PDF
│   └── Profile.pdf
├── .claude/
│   ├── plans/          # Local implementation plans
│   ├── agents/         # Agent configurations
│   └── rules/          # Domain-specific rules
└── ...config files
```

## Core Principles

1. **TYPE SAFETY** — All data typed, no `any`, strict mode
2. **KISS** — Simple, readable solutions. No over-engineering
3. **STATIC FIRST** — All CV data is static TypeScript. GitHub repos fetched at build time via SSG
4. **YAGNI** — Build what's needed now, not hypothetical features

## Development Workflow — Local Plan-Driven

1. Create plan in `.claude/plans/<feature-name>.md`
2. Break into phases with checkboxes
3. Work through phases sequentially
4. Update plan as work progresses
5. Commit with `/commit` after each phase
6. Mark plan complete when done

## Setup & Commands

```bash
npm install          # Install dependencies
npm run dev          # Start dev server (http://localhost:3000)
npm run build        # Production build
npm run start        # Start production server
npm run lint         # ESLint
```

## Code Conventions

- TypeScript strict mode — no `any`
- Functional components and hooks
- Self-documenting code — comments only for non-obvious logic
- `@/` path alias for imports from project root
- Tailwind CSS for all styling — no inline styles or CSS modules
- shadcn/ui components installed via CLI, customized in `components/ui/`

## Skill Auto-Loading

**MANDATORY:** Load the matching skill BEFORE starting work when a trigger matches.

| Trigger | Skill | When |
|---------|-------|------|
| Git commits | `/commit` | Any commit operation |
| Push + PR | `/commit-push-pr` | Pushing code or creating PRs |
| Brainstorming | `/brainstorming` | Before significant features or creative work |
| New feature | `/feature-dev` | Building features of significant scope |
| Code quality | `/simplify` | After implementation, review changed code |
| Next.js patterns | `/nextjs-app-router-patterns` | Server components, routing, SSR/SSG, layouts |
| shadcn/ui | `/shadcn-ui` | Installing components, building forms, theming |
| React performance | `/vercel-react-best-practices` | Performance work, refactoring React code |
| UI review | `/web-design-guidelines` | Reviewing UI code, accessibility checks |
| CV data update | `/cv-parser` (custom) | When `docs/Profile.pdf` changes, regenerate data |
| Web research | `/research` | Multi-source synthesis, design inspiration |
| Quick lookup | `/search` | Single-query web search |

### Design Skill Preference

Before any frontend UI creation task, ask:

> "Which design approach should I use?"
>
> 1. **`/frontend-design`** — Full page/component creation with bold aesthetics
> 2. **`/frontend-aesthetics`** — Lightweight design guardrails
> 3. **`/ui-ux-pro-max`** — Design planning and exploration

## MCP Servers

- `context7` — Library documentation lookup (Next.js, React, Tailwind, shadcn/ui)
- `shadcn` — Component search, examples, audit checklists

## Agent Protocols

Global agent definitions live in `~/.claude/agents/`. This project uses:

### Tester-Agent Protocol

- **When:** After UI changes, before claiming work is done
- **How:** Agent tool → `subagent_type: "general-purpose"`, `model: "sonnet"`
- **Queries:** `VERIFY page:/ Checks: <list>` or `FLOW: <scenario> Steps: 1. ... 2. ...`
- **Global config:** `~/.claude/agents/tester-agent/AGENT.md`
- **Local patterns:** `.claude/agents/tester-agent/test-patterns.md`

### Other Available Agents (global)

- **architect-agent** — `~/.claude/agents/architect-agent/AGENT.md` — Architecture knowledge base. Not needed for this project (simple static site) but available if complexity grows.
- **mobile-tester-agent** — `~/.claude/agents/mobile-tester-agent/AGENT.md` — Mobile simulator testing. Not applicable to this project.

## Development Mode — Superpowers

For non-trivial tasks, follow the full Superpowers pipeline:

`/brainstorming` → `/writing-plans` → `/test-driven-development` → domain skills → `/executing-plans` → `/verification-before-completion` → `/finishing-a-development-branch`

Skip for trivial changes (config tweaks, typo fixes).

## Cross-Cutting Recipes

- **New section/feature:** `/brainstorming` → `/writing-plans` → design skill → `/feature-dev` → `/shadcn-ui` + `/nextjs-app-router-patterns` → tester-agent → `/simplify`
- **Bug fixing:** `/superpowers:systematic-debugging` → fix → tester-agent → `/commit`
- **CV data update:** Update `docs/Profile.pdf` → `/cv-parser` → verify data files → `/commit`
- **Library APIs:** `context7` MCP → verify before writing code
- **Claiming done:** `npm run build` passes. Tester agent verifies. `/simplify` before committing.

## Git Workflow

- Use conventional commit messages (feat:, fix:, refactor:, docs:, chore:)
- **MANDATORY:** Use `/commit` for ALL git commits
- **MANDATORY:** Use `/commit-push-pr` for ALL push and PR operations
- Never commit directly to `main` — use feature branches
